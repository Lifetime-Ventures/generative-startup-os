{
  "name": "gsos-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Generative Startup OS — remote MCP server (no-auth, stateless prompt-CDN). Serves the GSOS commands as MCP prompts + onboarding conductor to Claude Desktop.",
  "scripts": {
    "build": "node scripts/build-mcp.mjs",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy",
    "dev": "npm run build && wrangler dev",
    "check": "npm run build && wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "agents": "^0.0.80"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
